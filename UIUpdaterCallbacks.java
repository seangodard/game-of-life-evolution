/**
 * An interface for an update thread to issue callbacks to display the next image.
 * @author Sean Godard
 */
public interface UIUpdaterCallbacks {

    /**
     * Display the next image of the simulation.
     */
    void showNext();
}
